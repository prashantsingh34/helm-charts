apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImageRepository
metadata:
  name: {{ include "weather.fullname" . }}
  namespace: flux-system
spec:
  image: {{ .Values.service.image.registry }}/{{ .Values.service.image.repository }}
  interval: 1m
  secretRef:
    name: docker-hub-cred
---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: {{ include "weather.fullname" . }}
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: {{ .Values.image.repository }}
  policy:
    semver:
      range: '>=0.0.1'
---